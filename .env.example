# Databricks-managed Claude endpoint configuration
# DO NOT commit .env — it contains secrets
#
# Replace the three values below with your actual workspace details.
# The .env creation step in the guide auto-fills the rest.

# Your Databricks workspace URL + serving endpoint path
ANTHROPIC_MODEL=databricks-claude-sonnet-4-5
ANTHROPIC_BASE_URL=https://<your-workspace>.cloud.databricks.com/serving-endpoints/<endpoint-name>
ANTHROPIC_AUTH_TOKEN=dapi<your-token-here>

# Required settings (do not change)
ANTHROPIC_CUSTOM_HEADERS=x-databricks-use-coding-agent-mode: true
CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1

# Git Bash path (auto-detected during install — update if Git is installed elsewhere)
CLAUDE_CODE_GIT_BASH_PATH=C:\Users\<YourName>\AppData\Roaming\Python\Python313\Scripts\git\usr\bin\bash.exe
